Function that converts hours to minutes:

function timeConvert(hours) {
  var minutes = hours * 60;
  return (minutes);
 }

----------------------------------------

Function that calculates the remainder of 2 numbers:

function findRemainder(x, y) {
  var result = x % y;
  return result;
}

----------------------------------------
